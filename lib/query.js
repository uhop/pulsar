// placeholder

// We should use ACME and Sizzle. The problem is they both are too big.
// One possible solution is to keep a simplified query library in the base, and allow to include other libraries
// explicitly. See: http://github.com/uxebu/embedjs/tree/master/src/query/ for possible alternatives.
// In any case we should provide light-weight alternatives as a source for generating a custom base.